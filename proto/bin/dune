(executable
  (name main)
  (public_name proto)
  (modules main)
  (libraries gg vg vgr_raster bimage bimage-unix bigarray)
  (flags (:standard -open StdLabels)))

(library
  (name vgr_raster)
  (modules vgr_raster)
  (libraries gg vg bigarray))
